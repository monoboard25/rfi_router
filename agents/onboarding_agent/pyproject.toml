[project]
name = "onboarding-agent"
version = "0.1.0"
dependencies = ["azure-functions", "openai", "pydantic", "requests", "pytest"]
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
